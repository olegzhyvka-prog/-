# Активний стан

Що виконується **зараз**. Один файл, переписується оркестратором при зміні кроку.

`SessionStart`-хук читає його і вливає в контекст нової сесії. `Stop`-хук комітить його
в git після кожного ходу — тому обірвана сесія не втрачає точку продовження.

**Це не журнал.** Історія — в git. Тут завжди лише поточне.

Коли задача завершена — скинути до шаблону («Задача: немає»).

---

---

## Задача

**Задача:** підсилення оркестратора і працівників зовнішніми матеріалами
**Робоча тека:** `company/workspace/2026-08-01-hermes-audit/` (етап 1)
**Проєкт:** інфраструктура компанії (НЕ symplexy — це не робота над бізнесом)
**Розпочато:** 2026-08-01

## Крок

**Поточний:** етап 1 (Hermes) закритий 8/8. Етап 2 (карусель) закритий 3/3.
Етап 3 (розширений обсяг) закритий. **Наступне — робота, а не інфраструктура: редизайн
сайту.** Чекаю від засновника адресу сайту й обсяг редизайну.

**Етап 3 — виконано 2026-08-02.** Засновник переглянув обсяг: «встанови всі потужні
скіли, особливо по дизайну» + дав три факти (реклама запускається · сайт є · треба
редизайн). Поставлено 108 скілів у 8 паках, разом у проєкті **163**:
дизайн `taste-skill`/`redesign-skill`/`impeccable`/`emilkowalski`/`gsap-*` · реклама
`ads-*` (33) · SEO `seo-*` (25) · `last30days`, `agent-browser` · соцконтент (13).
Рознесено по маршрутизації 6 працівників. Повний список відхиленого — `decisions.md`.

**Виправлено дефект:** `design`, `analytics`, `site-architecture` були перекриті
особистими скілами засновника і не працювали. Аліаси: `design-direction`,
`analytics-tracking`, `site-structure`.

**Етап 2 — виконано 2026-08-02** (деталі й повний список відхиленого — у
`company/ledger/decisions.md`, запис від 2026-08-02):
1. ✅ `blader/humanizer` → `.claude/skills/humanizer/` (MIT, без залежностей)
2. ✅ маркетинговий пак `coreyhaines31/marketingskills` 1.1.x → 2.x, 40 → 49 скілів;
   перевстановлено офіційним CLI, `skills-lock.json` вичищено (68 → 49 записів)
3. ✅ решта пака `nextlevelbuilder/ui-ux-pro-max`: design, design-system, brand,
   banner-design, slides, ui-styling; сам `ui-ux-pro-max` оновлено (13 → 22 стеки)
4. ✅ 17 перейменувань + злиття `page-cro`+`form-cro`→`cro` рознесено по 30 наших файлах
   за офіційною картою апстріму (`.agents/VERSIONS.md`)
5. ✅ `CLAUDE.md`, `README.md`, `AGENTS.md` приведено у відповідність до реального складу

**Відхилено 19 із 22** — щоб не розглядати вдруге, читати `decisions.md`, не карусель.

**Що засновник робить:** будував Hermes Agent (Nous Research) на своєму VPS разом із
ChatGPT — як супер-агента-кофаундера з памʼяттю і субагентами. Скидає мені результати
тих досліджень, щоб я брав із них цінне **собі й працівникам**. Hermes — окрема система,
з нею себе не порівнювати і не питати «хто головний». Задача проста: аудит матеріалу →
що цінне → поставити.

**Виконано (етап 1):**
1. ✅ Аудит у 3 частинах — `report.md`, `report-part2-sessions.md`, `report-part3-hermes-source.md`
2. ✅ `company/protocols/source-of-truth.md` — ієрархія джерел правди + статуси ✅🟡⬜❌⏸ у `decisions.md`
3. ✅ `company/workspace/ACTIVE.md` — цей файл
4. ✅ `session-brief.sh` розрізняє `source` (startup/resume/compact/fork)
5. ✅ `precompact.sh` — новий хук, зареєстрований у `settings.json`
6. ✅ `CLAUDE.md` 211 → 103 рядки; делегування перенесено в `orchestration.md`
   (причина: `CLAUDE.md` вантажиться в контекст усіх 21 працівника)
7. ✅ `definition-of-done.md` — блок «Пропоную в памʼять»: працівник пропонує, оркестратор вносить
8. ✅ `memory-guard.sh` — скан фактів бізнесу у файлах ремесла; протестовано в обидва боки

**Далі (етап 2):** засновник скидає **новий матеріал від ChatGPT**. Порядок той самий:
прочитати → звірити з фактами (документація Claude Code + стан репозиторію, НЕ з памʼяті) →
сказати що беру / що не беру і чому → показати список → поставити після його «так».

**Свідомо НЕ зроблено** (засновник не обрав): `disallowedTools` і `maxTurns` у frontmatter
21 працівника. Пропозиція лишається чинною — деталі в `report.md`, розділ C1.

## Незворотне вже зроблене

<!-- КРИТИЧНИЙ РОЗДІЛ. Усе, що не можна виконати вдруге:
     публікації, витрати, зовнішні запити, листи, зміни в проді, видалення.
     Порожньо = нічого незворотного не робилось. Не залишати порожнім за здогадом. -->

- Запушено в 4 гілки: `claude/agent-hypothesis-audit-plrzr9`, `main`,
  `claude/add-marketing-skills-6gIws`, `claude/digital-workers-solo-founder-b2zbxz`.
- Зроблено **злиття** `origin/main` у робочу гілку (там було 4 коміти, яких не було в мене).
  Force push НЕ робився — чужа робота ціла.
- Грошей не витрачено, назовні нічого не опубліковано, нічого не видалено.
- 2026-08-02: `npx skills add coreyhaines31/marketingskills` — тягнув пакети з npm і
  GitHub. Мережевий запит, не витрата. Старі 40 скілів замінено новими 49 — оборотно
  через git (`git revert`), не є втратою.

## Блокери й відкриті питання

**Головна дірка:** `company/projects/symplexy/founder-context.md` порожній —
21 працівник не знає бізнесу засновника. Жодна архітектурна зміна цього не лікує.

**Готовий інструмент для цього вже лежить:** `company/projects/symplexy/CONTEXT-REQUEST.md` —
блок, який засновник копіює в той чат GPT, де обговорював Symplexy. Відповідь віддає сюди,
оркестратор розкладає по файлах.

## Наступна безпечна дія

**Почати редизайн сайту.** Інструменти вже стоять. Бракує трьох речей від засновника:
1. **адреса сайту** — без неї `brand-designer` не має що аудитувати;
2. **що саме не влаштовує** в поточному вигляді (одне речення достатньо);
3. чи є межі: що НЕ чіпати (логотип, кольори, структура, CMS).

Порядок роботи, коли це буде: `brand-designer` вантажить `redesign-skill` → аудит
поточного сайту → напрям → макет → `fullstack-engineer` реалізує → `impeccable`
полірує. Реклама й SEO — окремі задачі, інструменти під них також уже стоять.

Паралельно лишається корисним дозаповнити `founder-context.md` (зараз 3 пункти з ~30).

---

## Як заповнювати

Оркестратор переписує цей файл:

- на старті задачі — заповнює всі розділи;
- при переході до наступного працівника — оновлює «Крок»;
- **одразу після незворотної дії** — дописує в «Незворотне», не відкладаючи;
- при завершенні — скидає до шаблону.

Правило одне: **розділ «Незворотне» пишеться до того, як дія виконана, а не після.**
Якщо сесія обірветься між дією і записом, наступна повторить публікацію або витрату.

Формат вільний, головне — щоб нова сесія за 10 секунд зрозуміла, де ви спинились.
