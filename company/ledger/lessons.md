# Уроки компанії

Уроки, що стосуються **кількох працівників або самої оркестрації**. Особисті уроки —
у `lessons.md` кожного працівника.

Сюди попадає урок, якщо:
- та сама помилка сталась у двох різних працівників;
- проблема була в тех-завданні, а не у виконавці;
- зламалась передача роботи між зонами;
- оркестратор (я) щось зробив не так.

Формат (нове зверху):

```
## YYYY-MM-DD — <заголовок>
**Що сталось:** факти.
**Кого торкнулось:** <slug'и>
**Справжня причина:** роль / тех-завдання / передача / оркестрація / контекст.
**Ціна:** час, гроші, довіра.
**ПРАВИЛО:** наказова форма.
**Що змінено в системі:** який файл протоколу або опису працівника оновлено.
```

Останній рядок обовʼязковий: урок без зміни в системі повторюється.

---

_(порожньо — перший урок після першої реальної роботи)_

## 2026-07-30 — Робота була невидима для інших сесій: не та гілка + немає README
**Що сталось:** засновник в іншому чаті спитав про працівників — той чат відповів, що
про них ніде не записано. Причина: репозиторій не має `main`, гілка за замовчуванням —
`claude/add-marketing-skills-6gIws` (стара, лише зі скілами). Уся компанія — на гілці
`claude/digital-workers-solo-founder-b2zbxz`. Нова сесія відкриває репо, потрапляє на
гілку за замовчуванням і бачить порожньо.

**Кого торкнулось:** оркестратора (мене).

**Справжня причина:** оркестрація. Я вважав «закомічено і запушено» = «записано і
доступно». Це різні речі: запушено на гілку, яку ніхто інший не відкриває. Плюс не було
`README.md` у корені — репозиторій не описував сам себе.

**Ціна:** втрата довіри засновника до того, що робота взагалі зберігається; час на розбір.

**ПРАВИЛО 1:** «записано» = видно тому, хто відкриє репозиторій **за замовчуванням**.
Після великої роботи перевіряти: `git remote show origin | grep "HEAD branch"` — і якщо
робота не там, сказати засновнику і запропонувати злиття.
**ПРАВИЛО 2:** у корені завжди `README.md`, який пояснює репозиторій людині й асистенту,
плюс явна вказівка робочої гілки в `CLAUDE.md`.

**Що змінено в системі:** створено `README.md`; у `CLAUDE.md` додано блок «Прочитай це
першим» і попередження про гілки.

---

## 2026-07-30 — Локальна копія відкотилась на перший коміт; врятував автокоміт
**Що сталось:** під час розбору виявилось, що локальний HEAD стоїть на найпершому коміті.
Файлів `audit.md`, `workspace.md`, `red-team-critic`, хуків і перейменування в `symplexy`
у контейнері не було. Віддалена гілка мала все (c6abf12). Відновлено через
`git reset --hard origin/...`.

**Справжня причина:** середовище ефемерне; локальний стан не є джерелом правди.

**Ціна:** нульова — лише тому, що кожен крок комітився і пушився одразу.

**ПРАВИЛО:** джерело правди — **віддалена гілка**, не файли в контейнері. Перед роботою
над наявною системою звіряти локальний HEAD із `origin/<гілка>`; розбіжність → спершу
`fetch` + `reset --hard`, потім робота. Ніколи не судити про стан системи за тим, що
лежить у контейнері.

**Що змінено в системі:** попередження в `CLAUDE.md`; Stop-хук з автокомітом і пушем
підтвердив свою потрібність на практиці — не чіпати.

## 2026-07-30 — Токен Hugging Face закомічено відкритим текстом в іншій гілці
**Що сталось:** під час зведення гілок виявлено, що інша сесія засновника закомітила
робочий токен HF у `.claude/settings.json` (гілка `claude/new-session-Elvm4`,
коміт `c303480`). Значення тут навмисно не наводиться.

**Кого торкнулось:** усю компанію — репозиторій спільний.

**Справжня причина:** робота велась паралельно в кількох сесіях, кожна у своїй гілці.
Наш Stop-хук зі сканером секретів **заблокував би цей коміт** — але хук існує лише на
гілці компанії. Сесія без нього працювала без захисту.

**Ціна:** токен скомпрометований, потребує ротації. Видалення файлу не допомагає —
значення назавжди лишається в історії коміта.

**ПРАВИЛО 1:** секрети ніколи не потрапляють у `settings.json` (він комітиться).
Тільки в `.claude/settings.local.json` — він у `.gitignore`.
**ПРАВИЛО 2:** знайдений секрет = **ротація**, не видалення. Прибрати з файлу
недостатньо; поки токен живий, він скомпрометований.
**ПРАВИЛО 3:** фрагментація по гілках небезпечна не лише втратою видимості — вона
позбавляє інші сесії наших захисних хуків. Робота має зводитись в одну гілку.

**Що змінено в системі:** створено `.gitignore` із захистом секретів;
`.claude/settings.local.json.example` як зразок правильного місця для ключів;
явна перевірка, що `.claude/agent-memory/` НЕ в ігнорі.

## 2026-08-01 — Автокоміт-хук синхронізував лише одну гілку; дзеркала відставали
**Що сталось:** знайдено під час приймального тестування системи. Stop-хук комітив і
пушив памʼять лише в поточну гілку. `main` і гілка за замовчуванням відставали після
кожного автозбереження.

**Кого торкнулось:** усю компанію.

**Справжня причина:** зведення гілок було зроблено як **разова дія**, а не як властивість
системи. Все, що тримається на тому, що хтось згадає повторити вручну, розсинхронізується.

**Ціна:** нульова — зловлено на тестуванні до реальної роботи. Якби не зловили: нова
сесія на `main` бачила б компанію, але зі **застарілою памʼяттю** — без уроків,
записаних після останнього ручного зведення. А памʼять і є весь сенс системи.

**ПРАВИЛО:** одноразове виправлення стану — не виправлення. Якщо властивість має
триматись у часі, її забезпечує механізм, а не намір. Після кожного «звели докупи»
питати: що поверне це назад, і що цьому завадить?

**Що змінено в системі:** `.claude/hooks/memory-guard.sh` після пушу робочої гілки
дзеркалить HEAD у `main` і гілку за замовчуванням. Push звичайний, не force — якщо
гілка розійшлась, він просто не пройде і нічого не зіпсує.

## 2026-08-01 — Правдоподібне число в шаблоні читається як факт
**Що сталось:** засновник побачив у виводі `MRR: 4200 USD` і вирішив, що хтось вигадав
дохід його бізнесу. Насправді це був рядок-приклад із шаблону памʼяті, який я сам написав:
зразок формату «число + дата + джерело». Реальні факти в тому файлі були «невідомо».

**Кого торкнулось:** усі 21 працівник — рядок стояв у шаблоні `projects/*/state.md`.

**Справжня причина:** оркестрація. Я поставив у навчальний зразок конкретну правдоподібну
цифру. Мій же протокол попереджає: «здогад у памʼяті через місяць виглядає як факт» —
і зразок працює так само. Через три місяці цей рядок прочитався б як дані.

**Ціна:** втрата довіри засновника на рівному місці; час на пояснення.

**ПРАВИЛО:** у шаблонах, зразках і прикладах **не ставити правдоподібних значень**.
Плейсхолдер `<число>`, `<дата>`, `<джерело>` сплутати з даними неможливо, конкретну
цифру — можна.

**Другий урок, для мене:** перевірка на вигадані числа (`grep`) дала хибне спрацювання
саме на цьому шаблоні. Автоматична перевірка, що кричить на власні шаблони, привчає
ігнорувати її попередження. Перевірка має відрізняти шаблон від запису.

**Що змінено в системі:** замінено на плейсхолдери у 21 файлі памʼяті і в
`company/protocols/memory-protocol.md`, куди додано явне правило про приклади.

## 2026-08-01 — Я заявив, що сканер секретів зловив би токен. Він би не зловив.
**Що сталось:** знайшовши закомічений HF-токен, я двічі сказав засновнику, що наш
Stop-хук «зловив би це і заблокував коміт». `qa-lead` на приймальному тестуванні
поставив це під сумнів (дефект Д1). Перевірка підтвердила його: шаблону `hf_` у
сканері не було взагалі, а без `-i` він не бачив `HF_TOKEN`, `API_KEY`, `Password`.
На 5 реалістичних секретах — **0 влучань**.

**Кого торкнулось:** оркестратора (мене) і всю компанію — це був останній рубіж перед
автокомітом памʼяті в git.

**Справжня причина:** я написав захисний механізм і **жодного разу не перевірив його на
реальному секреті**. Далі заявив про його спрацювання як про факт. Це рівно те
порушення, яке заборонене першим правилом компанії — заявити неперевірене.

**Ціна:** нульова цього разу (токен уже мертвий). Але система півдня жила з діркою в
захисті, у яку я вірив.

**ПРАВИЛО 1:** захисний механізм не існує, поки не перевірений **на тому, від чого
захищає**. Написати regex ≠ мати сканер.
**ПРАВИЛО 2:** перевіряти в обидва боки — і що ловить справжнє, і що НЕ чіпає своє.
Перевірка з хибними спрацюваннями буде вимкнена або зігнорована.
**ПРАВИЛО 3 (для мене як оркестратора):** не заявляти про поведінку власних хуків із
памʼяті про те, як я їх писав. Запустити і показати вивід.

**Що змінено в системі:** сканер переписано — 11 шаблонів провайдерів (`hf_`, `sk-ant`,
`gh[pousr]_`, `AKIA`, `sk_live`, `xox`, `AIza`, `glpat`, приватні ключі) + `-i` +
неквотовані значення. Перевірено: 8/8 справжніх секретів зловлено, 0/6 хибних спрацювань,
0 на чистому репозиторії. E2E: підкинутий токен **заблокував автокоміт**.

## 2026-08-02 — Я назвав симлінки дублікатами і мало не «прибрав» 40 скілів
**Що сталось:** розбираючи склад скілів, я запустив `diff -rq .agents/skills .claude/skills`,
отримав «ідентичні» по всіх 40 і доповів засновнику: «40 байт-в-байт дублікатів, треба
прибрати». Насправді `.claude/skills/*` — це **симлінки** в `.agents/skills/`, які створює
CLI `npx skills` при встановленні пака. `diff` іде за симлінком і чесно каже «однакове».
Видалити «дублікат» означало б знести всі 40 маркетингових скілів. Помилку зловив лише
тому, що перед видаленням пішов перечитати, чи хтось на ці теки посилається — і натрапив
на власний запис у `company/workspace/2026-08-01-system-test/findings.md`, де вже було
написано «symlink, не дублікат».
**Кого торкнулось:** оркестратор.
**Справжня причина:** контекст — я зробив висновок про **структуру** файлової системи
інструментом, який показує **вміст**.
**Ціна:** хибна доповідь засновнику, яку довелось прилюдно виправляти. Якби він сказав
«прибирай» одразу — мінус 40 скілів.
**ПРАВИЛО 1:** висновок «це дублікат» робиться через `ls -la` / `find -type l`, а не через
`diff`. `diff` порівнює вміст і мовчки йде за симлінком.
**ПРАВИЛО 2:** перед тим як щось видаляти, шукати згадки цього шляху в репозиторії. Свій
попередній запис — теж джерело; він там уже був і був правий.
**ПРАВИЛО 3:** те, що поставив пакетний менеджер, ним же й оновлюється. Я почав копіювати
теки руками — і отримав `skills-lock.json`, який описував неіснуючий стан (68 записів на
49 скілів). Виправилось лише перезапуском `npx skills add`.
**Що змінено в системі:** `CLAUDE.md` — рядок про скіли тепер прямо каже, що в
`.claude/skills/` маркетингові лежать симлінками, і що пак оновлюється тільки через
`npx skills add coreyhaines31/marketingskills -p`, не руками.

## 2026-08-02 — Рекламна карусель зі списком інструментів — не аудит
**Що сталось:** засновник дав 8 картинок каруселі «95% користувачів Claude не знають про
ці skills» з 23 репозиторіями. Опис у картинці і вміст репозиторію збіглись не завжди.
Найгірший приклад: `social-media-skills` продано як «free collection of skills for social
content» — усередині 59 згадок LinkedIn проти 9 YouTube, три скіли написані під конкретну
людину («Charlie Hills' signature style»), а два ключові вимагають платних `APIFY_API_TOKEN`
і `GOOGLE_AI_API_KEY`. `caveman` обіцяє «−65% токенів», не кажучи, що платиш за це стилем
відповіді. Із 23 репозиторіїв реально придатними виявились 3.
**Кого торкнулось:** оркестратор; опосередковано — усі 21, бо кожен скіл вантажиться в їхній
контекст.
**Справжня причина:** контекст — джерелом списку був маркетинговий контент, а не аудит.
**Ціна:** ~40 хвилин на перевірку. Без неї — +160 скілів у контексті кожного працівника.
**ПРАВИЛО 1:** інструмент зі списку оцінюється за вмістом репозиторію, а не за описом у
пості. Мінімум: чи існує · ліцензія · чи потребує ключів або грошей · чи не дублює наявне.
**ПРАВИЛО 2:** «поставити все» — окремий вид шкоди. Кожен скіл коштує контексту всім
працівникам і псує влучність тригерів. Ціна додавання називається вголос разом із користю.
**ПРАВИЛО 3:** коли зовнішній ресурс блокує egress-політика (403/407) — не обходити.
Шукати дозволений шлях (тут — git-проксі замість `curl`) або доповісти, що заблоковано.
**Що змінено в системі:** запис у `company/ledger/decisions.md` від 2026-08-02 фіксує, що
саме відхилено і чому — щоб наступна карусель із тими самими назвами не розглядалась знову.

## 2026-08-02 — Три скіли були недосяжні: особистий перекриває проєктний
**Що сталось:** після встановлення пака `ui-ux-pro-max` я доповів, що скіл `design`
поставлено. Формально файл лежав на місці. Насправді він був **недосяжний**: у засновника
є особистий скіл із такою самою назвою в `~/.claude/skills/design`, а рівні мають
пріоритет — enterprise > personal > project. Те саме мовчки сталося з `analytics`
(маркетинговий трекінг перекритий особистим бізнес-аналізом) і `site-architecture`.
Три скіли рахувались встановленими і не працювали.
**Кого торкнулось:** `brand-designer`, `seo-aeo-lead`, `data-analyst`, `paid-ads-buyer`.
**Справжня причина:** контекст — я перевірив наявність файлу і вирішив, що це те саме,
що доступність скіла.
**Ціна:** доповідь «поставлено» була неправдою протягом одного дня.
**ПРАВИЛО 1:** «файл на місці» ≠ «скіл доступний». Після встановлення звіряти назви:
`comm -12 <(ls ~/.claude/skills|sort) <(ls .claude/skills|sort)` — усе, що вивелось,
перекрито і не працює.
**ПРАВИЛО 2:** зіткнення лікується аліасом, а не перейменуванням. Симлінк під вільною
назвою поруч зберігає внутрішні перехресні посилання пака цілими.
**ПРАВИЛО 3:** перевіряти назви **до** копіювання, а не після. Це коштує одну команду і
знімає весь клас помилки.
**Що змінено в системі:** створено аліаси `design-direction`, `analytics-tracking`,
`site-structure`; у `CLAUDE.md` доданий абзац про перекриття рівнів і вимогу звіряти
назву з `ls ~/.claude/skills` перед додаванням скіла; маршрутизація `brand-designer` і
`seo-aeo-lead` вказує саме на аліаси.

## 2026-08-02 — Відсутній контекст блокував менше, ніж я думав
**Що сталось:** вранці я відклав рекламний, SEO і дизайн паки з формулюванням «не знаю,
чи Symplexy запускає рекламу і чи має сайт». Засновник відповів одним рядком: реклама
запускається, сайт є, потрібен редизайн. Три факти, десять секунд його часу — і рішення
стало очевидним. Я тримав питання закритим півдня замість того, щоб просто спитати ці
три речі разом зі списком.
**Кого торкнулось:** оркестратор.
**Справжня причина:** оркестрація — я поставив питання «яким буде обсяг», але не поставив
дешевшого питання, від якого обсяг залежав.
**Ціна:** півдня затримки на роботі, яку засновник хотів зробити одразу.
**ПРАВИЛО 1:** коли рішення впирається у відсутній факт — питати **сам факт**, а не
пропонувати засновнику вибирати обсяг наосліп. «Ти запускаєш рекламу?» дешевше за
«обери один із чотирьох варіантів».
**ПРАВИЛО 2:** відсутність контексту — привід поставити конкретне питання, а не привід
відкласти роботу. Відкладати можна лише те, що без факту буде **зроблено неправильно**,
а не те, що без факту просто некомфортно вирішувати.
**ПРАВИЛО 3:** кожен факт, який засновник називає мимохідь у розмові, одразу йде в
`founder-context.md`. Інакше наступна сесія знову скаже «невідомо».
**Що змінено в системі:** три факти записані в
`company/projects/symplexy/founder-context.md` (розділи 2, 4, 7) із датою і позначкою,
що саме залишилось невідомим поруч із кожним.
