# Реєстр рішень компанії

Рішення рівня компанії: ті, що зачіпають кілька зон, скасовані вето і відкриті питання
до засновника. Особисті рішення працівників — у їхніх `decisions.md`.

Мета: **не переглядати закриті питання по колу** і через рік розуміти *чому* так зроблено.

## Статус — перший рядок кожного запису

Без статусу запис читається як **🟡 рекомендація**, а не як рішення. Це навмисно:
відсутність позначки не дає переваги. Повні правила — `company/protocols/source-of-truth.md`.

| Статус | Значення | Можна діяти? |
|---|---|---|
| ✅ **ухвалено** | засновник сказав «так» | так |
| 🟡 **рекомендація** | працівник або оркестратор пропонує | **ні** — спочатку спитати |
| ⬜ **розглядається** | винесено, відповіді ще немає | ні |
| ❌ **відхилено** | сказано «ні» | ні — і не пропонувати знову без нових даних |
| ⏸ **застаріло** | обставини змінились | ні — спочатку перевірити |

Формат (нове зверху):

```
## YYYY-MM-DD — <рішення>
**Статус:** ✅ ухвалено | 🟡 рекомендація | ⬜ розглядається | ❌ відхилено | ⏸ застаріло
**Хто вирішив:** засновник / <slug> / спільно
**Контекст:** що змусило вирішувати.
**Варіанти:** A (плюси/мінуси) · B · C
**Рішення:** що вибрано.
**Причина:** чому саме це.
**Наслідки:** що свідомо приймаємо як мінус.
**Перегляд:** дата або умова, за якої повертаємось.
```

Для скасованого вето додатково:
```
**Скасоване вето:** <slug> заблокував <що> через <причину>.
**Рішення засновника:** діємо всупереч, ризик прийнято свідомо.
```

---

## 2026-08-02 — Обсяг переглянуто: поставлено 8 паків, 108 нових скілів
**Статус:** ✅ ухвалено
**Хто вирішив:** засновник — прямою вказівкою «встанови всі потужні скіли, особливо по
дизайну», після того як я вже назвав ціну в контексті
**Контекст:** того ж дня засновник дав три факти, яких бракувало для рішення: **реклама
запускається · сайт є · потрібен редизайн**. Саме через їх відсутність я вранці відклав
рекламний, SEO і дизайн паки. Підстава відкладати зникла.
**Рішення:** поставлено 108 скілів у восьми паках:
- дизайн (пріоритет): `taste-skill` (9 — зокрема `redesign-skill`), `emilkowalski/skills`
  (8), `gsap-skills` (8), `impeccable` (1)
- реклама: `claude-ads` (33 скіли `ads-*`)
- SEO: `claude-seo` (25 базових `seo-*`, без 8 extensions під платні сервіси)
- дослідження: `last30days` (1), `agent-browser` (1)
- соцконтент: `social-media-skills` (13 із 17)
Разом у проєкті **163 скіли**.
**Причина:** ціна в контексті названа й прийнята засновником свідомо. Три нові факти
перетворили «невідомо, чи знадобиться» на «знадобиться зараз».
**Не поставлено, і чому** (мінус — це відсутність робочого стану, не смак):
- `caveman`, `ponytail`, `i-have-adhd` — перемикачі стилю відповіді, перебивають формати
  доповідей компанії;
- `codex-plugin-cc` — окрема підписка OpenAI; `claude-hud`, `graphify` — потребують ключів;
- `hyperframes` — 132 МБ + FFmpeg + Docker, не переживе контейнер;
- `remotion` — **у репозиторії немає файлу ліцензії** (`private: true`); чуже без ліцензії
  в репозиторій засновника не комітимо;
- `find-skills` — дублює вбудований пошук скілів;
- з `taste-skill` прибрано `gpt-tasteskill`, `image-to-code-skill`, `stitch-skill`
  (зроблені під Codex і Google Stitch) і `taste-skill-v1` (застаріла версія);
- з `social-media-skills` прибрано `reels-scripting` і `post-scorer` (платні
  `APIFY_API_TOKEN`, `GOOGLE_AI_API_KEY`), `pinned-comment` (написаний під конкретну
  людину), `niche-research` (конфлікт назви з особистим скілом засновника).
**Наслідки:** 108 скілів рознесено по маршрутизації пʼятьох працівників
(`brand-designer`, `paid-ads-buyer`, `seo-aeo-lead`, `fullstack-engineer`,
`content-strategist`, `market-researcher`) — інакше вони лежали б мертвим вантажем.
Окремо зафіксовано: `ads-launch` і `ads-optimize` вміють **застосовувати** зміни в
рекламних акаунтах — це гроші, тільки чернетка + підтвердження засновника.
**Перегляд:** після першого реального прогону редизайну — які з дизайн-скілів
спрацювали, а які лише забивають контекст.

## 2026-08-02 — З каруселі «22 skills» узято 3 пункти, 19 відхилено
**Статус:** ✅ ухвалено
**Хто вирішив:** засновник (обрав «мінімум») після аудиту оркестратора
**Контекст:** засновник дав 8 картинок рекламної каруселі (`@swiperightai`) зі списком
23 GitHub-репозиторіїв зі скілами. Перевірено: усі 23 існують (`git ls-remote`), 21
клоновано і прочитано зсередини. `curl` до github блокує egress-політика (403) — не
обходили, пішли через git-проксі.
**Варіанти:** A — поставити все (≈+160 скілів) · B — мінімум із 3 пунктів ·
C — спершу заповнити `founder-context.md`.
**Рішення:** B. Поставлено: `blader/humanizer`; оновлено маркетинговий пак
`coreyhaines31/marketingskills` 1.1.x → 2.x (40 → 49 скілів); додано решту пака
`nextlevelbuilder/ui-ux-pro-max` (6 скілів: design, design-system, brand,
banner-design, slides, ui-styling).
**Причина:** у компанії вже було ~130 скілів. Опис кожного вантажиться в контекст усіх
21 працівника, тож «поставити все» зробило б систему гіршою, а не кращою. Карусель —
engagement-контент («95% не знають»), а не аудит.
**Відхилено і чому:**
- `caveman` — «−65% токенів» ціною телеграфного стилю: б'є по правилу №1 (повнота
  доповіді) і по українській комунікації.
- `ponytail`, `i-have-adhd` — стилі виводу; конфліктують із нашими форматами доповідей.
- `codex-plugin-cc` — окрема підписка OpenAI (гроші, правило №5) + дублює
  `fullstack-engineer`.
- `claude-hud`, `graphify`, `impeccable` — потребують API-ключів (гроші).
- `claude-hud` додатково безсенсовний у хмарному контейнері: HUD нема де показувати.
- `agent-browser` — дублює наявні WebFetch/WebSearch і Chromium у контейнері.
- `hyperframes` — 175 МБ + FFmpeg + Docker; не переживе контейнер.
- `social-media-skills` (charlie947) — карусель називає його «free collection для
  соцконтенту». Факт із репо: 59 згадок LinkedIn проти 9 YouTube, три скіли написані
  під конкретну людину, а `reels-scripting` і `post-scorer` вимагають `APIFY_API_TOKEN`
  і `GOOGLE_AI_API_KEY`. Symplexy — відео, не LinkedIn.
- `claude-ads` (33 скіли), `claude-seo` (25 + 8 платних extensions) — якісні, MIT, але
  дублюють `paid-ads-buyer` і `seo-aeo-lead`; вибірково — лише коли будуть відомі
  реальні канали з `founder-context.md`.
- `remotion`, `gsap`, `emilkowalski`, `taste-skill` — для тих, хто будує сайти й
  React-анімації; чи є що будувати — невідомо, поки `founder-context.md` порожній.
- `find-skills` (vercel-labs) — дублює вбудований пошук скілів.
**Наслідки:** маркетинговий пак 2.0 перейменував 17 скілів і злив `page-cro` + `form-cro`
у `cro` — посилання в 30 наших файлах оновлено за офіційною картою апстріму
(`.agents/VERSIONS.md`), а не за здогадом. `ui-styling` тягне 5.5 МБ шрифтів TTF у репо;
якщо це заважає — його можна прибрати окремо, решта пака від нього не залежить.
**Перегляд:** після заповнення `founder-context.md` — повернутись до `claude-ads`,
`claude-seo` і дизайн-паків уже з фактами про канали й продукт.

## 2026-07-30 — Створено штат з 20 цифрових працівників
**Статус:** ✅ ухвалено
**Хто вирішив:** засновник
**Контекст:** потрібна постійна команда для solo-засновника — з памʼяттю, особистістю
та накопиченням досвіду, а не одноразові помічники.
**Рішення:** 20 працівників у `.claude/agents/` з `memory: project`; памʼять у git;
місячний протокол перенавчання; протоколи оркестрації, передачі, приймання та ескалації.
**Причина:** памʼять у файлах — єдиний спосіб, щоб команда не починала з нуля щоразу
і не повторювала помилок. Git — щоб памʼять переживала перезапуск контейнера.
**Наслідки:** дисциплiна оновлення памʼяті обовʼязкова; без неї система деградує до
звичайних субагентів.
**Перегляд:** після першого місяця роботи — чи памʼять реально наповнюється.

## 2026-07-30 — Додано механічний контроль дисципліни (хуки) і 21-го працівника
**Статус:** ✅ ухвалено
**Хто вирішив:** засновник
**Контекст:** система тримається на тому, що працівник сам оновить памʼять і сам не
вигадає число. Це було прописано як правило, але нічим не перевірялось — через місяць
роботи дисциплiна попливла б непомітно. Окремо: усі 20 працівників за замовчуванням
допомагають, тобто знаходять спосіб зробити те, що попросили. Опонента не було.
**Варіанти:** A — залишити на чесному слові · B — хуки + опонент · C — лише опонент
**Рішення:** B.
1. `.claude/hooks/memory-guard.sh` (Stop): ліміт 200 рядків MEMORY.md, скан секретів,
   автокоміт+пуш памʼяті. При підозрі на секрет коміт блокується.
2. `.claude/hooks/worker-memory-check.sh` (SubagentStop): попередження, якщо працівник
   завершив роботу, не змінивши памʼяті.
3. `red-team-critic` (Ілля Вороний) — адвокат диявола, без права вето.
**Причина:** памʼять живе лише в git — контейнер ефемерний, обірвана сесія без коміту
означала б втрату всього написаного за неї. Опонент закриває сліпу зону solo-засновника:
немає нікого, хто скаже «зачекай».
**Наслідки:** автокоміт створює технічні коміти в історії — прийнятна ціна за те, що
памʼять не зникає. Критик витрачає запуск на кожне велике рішення.
**Перегляд:** через місяць — чи спрацьовують хуки, чи калібрований критик.

## 2026-07-30 — Запозичено робочий цикл з Manus і Kimi; відхилено самописні скіли
**Статус:** ✅ ухвалено
**Хто вирішив:** засновник (запит «що ще додати, може запозичити в Manus/Hermes/Kimi»)
**Контекст:** штат і памʼять збудовані, але робочий цикл усередині запуску був слабким:
на довгій задачі ціль дрейфує, ланцюг працівників спілкується лише через переказ
оркестратора, невдалі спроби зникають без сліду.

**Взято:**
- **Manus, рецитація цілей** — працівник переписує чекліст у `task.md` по ходу роботи.
  Повертає ціль у кінець контексту, де увага моделі найсильніша. Лікує дрейф.
- **Manus, «keep the wrong stuff in»** — невдалі спроби не видаляються (`dead-ends.md`).
  Видима помилка зміщує модель від повторення; прибрана — гарантує повтор.
- **Manus, файлова система як контекст** — вже було в архітектурі памʼяті, підтверджено.
- **Kimi K2, чергування думка → дія → перевірка** — окремий крок самоперевірки перед
  звітом: пройти критерій «готово» по пунктах з доказом, а не з памʼяті про наміри.

**Відхилено:**
- **Hermes (Nous Research), «neutral alignment» / зниження відмов** — філософія цієї
  моделі в максимальній кермованості системним промптом і мінімумі відмов. Нам це
  шкідливе: червоні лінії працівників (не вигадувати числа, не робити незворотного без
  підтвердження, вето безпеки) — це і є цінність системи, а не тертя.
- **Самописні скіли працівників.** На SkillsBench скіли, написані людьми, дали +16,2
  пункти до частки успішних виконань; скіли, згенеровані моделлю, — нуль вимірюваного
  приросту (arxiv.org/html/2606.11435v1). Модель, що пише інструкцію собі, вкладає в неї
  те, що вже знає. Тому зростання йде через плейбуки (перевірена практика з цифрами),
  а працівник може лише *запропонувати* скіл — створює засновник.

**Наслідки:** робочі теки додають ceremony — тому заводяться лише для ланцюгів і
довгих задач, не для дрібниць.
**Перегляд:** через місяць — чи справді використовуються теки, чи стали формальністю.

## 2026-07-30 — Третій рівень маршрутизації і затвердження плану (з DeerFlow)
**Статус:** ✅ ухвалено
**Хто вирішив:** засновник (приніс патерн SuperAgent Harness з DeerFlow)
**Контекст:** засновник запропонував перебудувати систему під схему DeerFlow —
оркестратор + чотири під-агенти з таблицею «задача → агент».

**Що вже було:** сам патерн SuperAgent Harness реалізований від початку —
CLAUDE.md як оркестратор, 21 працівник у `.claude/agents/`, таблиця маршрутизації в
`ROSTER.md`, ланцюги в `orchestration.md`. Перехід на чотирьох агентів був би відкатом.

**Що засновник побачив правильно:** скіли справді були плоскими. Ієрархія була на рівні
агентів, але не навичок: працівник бачив список скілів через кому і мусив вгадувати,
який вантажити під підзадачу. У `content-strategist` таких скілів дев'ять.

**Рішення:**
1. **Третій рівень маршрутизації.** У розділі 5 `MEMORY.md` кожного з 21 працівника —
   таблиця «підзадача → скіл → примітка». Ланцюг тепер повний:
   засновник → оркестратор (ROSTER) → працівник (таблиця скілів) → скіл.
   Явно сказано: немає збігу — працюй без скіла; вантажити «про всяк випадок» шкідливо.
2. **Затвердження плану до запуску** (з DeerFlow human-in-the-loop): для ланцюга з
   трьох і більше працівників показую план ДО витрати запусків — хто, в якому порядку,
   що дає, чого не робимо, що я припустив.

**Причина 2:** раніше можна було запустити чотирьох і лише в кінці зʼясувати, що
засновник мав на увазі інше. Одне його речення до старту дешевше за чотири переписаних
артефакти після.

**Наслідки:** MEMORY.md виріс до ~114 рядків (ліміт 200 — запас є). План додає один
крок узгодження, тому не показується для одного-двох працівників.
**Перегляд:** через місяць — чи справді працівники вантажать скіли за таблицею.

## 2026-07-30 — Проєкт названо symplexy; аудит зроблено протоколом, а не працівником
**Статус:** ✅ ухвалено
**Хто вирішив:** засновник
**Контекст:** уточнено назву продукту — Symplexy.ai (раніше в теках було `simplex`).
Окремо: питання, чи потрібен працівник-аудитор для сайту, дизайну, бізнес-моделі.

**Рішення 1:** перейменовано `simplex` → `symplexy` у теці компанії і в памʼяті всіх
21 працівника. Зроблено зараз, поки контекст порожній — пізніше довелось би переносити
накопичені факти.

**Рішення 2:** аудитора **не додаємо**. Замість нього — `company/protocols/audit.md`
і команда `/audit`.
**Варіанти:** A — 22-й працівник-аудитор · B — протокол + скіл · C — нічого
**Причина вибору B:**
- аудит уже покритий: 14 зон, кожну аудитує профільний працівник із 25 роками в ній.
  Універсал дав би поверхневий зріз замість глибокого по кожній зоні;
- синтез теж має власника — `chief-of-staff` з Theory of Constraints у доктрині;
- бракувало не людини, а процедури: зведення, наскрізної пріоритезації і порівняння
  з минулим аудитом.
**Найсильніший аргумент ПРОТИ нашого вибору** (і як закритий): спеціаліст, який аудитує
власну роботу, перевіряє власне домашнє завдання. Закрито правилом — працівник позначає,
що з перевіреного робив сам за останні 3 місяці, і цю частину дивиться `red-team-critic`
або суміжний працівник. Без позначки аудит зони не приймається.
**Наслідки:** аудит вимагає кількох хвиль запусків — тому за замовчуванням точковий
(`/audit сайт`), а не повний.
**Ключова механіка:** звіти зберігаються з датами в `projects/<проєкт>/audits/`.
Знахідка, яку ігнорують третій аудит поспіль, піднімається до засновника окремо —
або виправляємо, або свідомо приймаємо ризик із записом.

## 2026-07-30 — Три шари самопредставлення репозиторію
**Статус:** ✅ ухвалено
**Хто вирішив:** засновник («треба, щоб у кожному новому чаті ти відразу розумів масштаб
своїх можливостей»)
**Контекст:** робота була закомічена й запушена, але нова сесія її не бачила — потрапляла
на гілку за замовчуванням, де немає ні `CLAUDE.md`, ні `company/`.

**Як це роблять інші (перевірено, не з голови):**
- `CLAUDE.md` — базові правила, вантажаться автоматично при старті в директорії;
- `AGENTS.md` — той самий гайд, нейтральний до інструменту; конвенція 2026 року, щоб
  його читали й не-Claude агенти;
- **SessionStart-хук** — за документацією Claude Code це «project-scoped, committed,
  automatically-applied еквівалент `--append-system-prompt`»: stdout вливається прямо
  в контекст моделі на старті сесії.

**Рішення:** зроблено всі три.
1. `.claude/hooks/session-brief.sh` (SessionStart) — друкує стан: скільки працівників,
   реєстр, команди, стан памʼяті, активний проєкт і чи заповнений його контекст, а також
   **попередження, якщо сесія на неправильній гілці**, з готовою командою переходу.
2. `AGENTS.md` у корені.
3. `README.md` у корені (додано раніше цього дня).

**Важлива деталь:** перша версія хука друкувала «21 процедур у плейбуках», хоча плейбуки
порожні — це були шаблонні заголовки. Хук порушував головне правило компанії (не заявляти
неперевіреного) на старті кожної сесії. Виправлено: тепер за порожньої памʼяті він прямо
пише «памʼять ще порожня, це нормально».

**Наслідки:** брифінг витрачає ~20 рядків контексту на кожну сесію. Прийнятна ціна за те,
щоб жодна сесія не починала з нуля і не перебудовувала наявне.
**Перегляд:** якщо брифінг почне застарівати — він рахує все динамічно, тому не має.

## 2026-07-30 — Роботу зведено в одну лінію; компанія видима з будь-якої гілки
**Статус:** ✅ ухвалено
**Хто вирішив:** засновник («спочатку токен, потім решта» — токен закрито, далі зведення)
**Контекст:** робота була розпорошена на чотири гілки. Нові сесії потрапляли на гілку за
замовчуванням і бачили порожній репозиторій. Крім втрати видимості це означало, що інші
сесії працювали **без наших захисних хуків** — саме тому в одній із них токен потрапив
у коміт безперешкодно.

**Стан токена:** на акаунті Hugging Face не лишилось жодного активного токена
(перевірено засновником) — скомпрометований мертвий. Гілку `claude/new-session-Elvm4`
не чіпали і не зливаємо: значення назавжди в її історії.

**Зроблено:**
1. Скіл `ui-ux-pro-max` перенесено з `claude/setup-dev-skills-0b0Sl` (без `__pycache__`,
   просканований на секрети) і вписано в маршрутизацію `brand-designer` та
   `fullstack-engineer`.
2. Компанію злито в гілку за замовчуванням — **fast-forward**, перевірено двічі перед
   пушем: у ній не було жодного унікального коміта, тож переписати щось було неможливо.
3. Створено `main` з тим самим станом.

**Наслідок:** усі три гілки на одному коміті. Нова сесія бачить компанію, хоч куди
потрапить, і одразу отримує SessionStart-брифінг та захисні хуки.

**Виправлено побічно:** брифінг сварився на «неправильну гілку» за жорстко зашитою
назвою — після зведення це стало неправдою. Тепер він перевіряє реальну проблему:
чи не відстала локальна копія від віддаленої.

**Що лишилось засновнику:** перемкнути гілку за замовчуванням на `main` у налаштуваннях
GitHub (Settings → Branches). Не терміново — компанія однаково видима звідусіль.
