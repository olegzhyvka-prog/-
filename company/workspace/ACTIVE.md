# Активний стан

Що виконується **зараз**. Один файл, переписується оркестратором при зміні кроку.

`SessionStart`-хук читає його і вливає в контекст нової сесії. `Stop`-хук комітить його
в git після кожного ходу — тому обірвана сесія не втрачає точку продовження.

**Це не журнал.** Історія — в git. Тут завжди лише поточне.

Коли задача завершена — скинути до шаблону («Задача: немає»).

---

## Задача

**Задача:** аудит гіпотез Hermes + впровадження погоджених змін
**Робоча тека:** `company/workspace/2026-08-01-hermes-audit/`
**Проєкт:** інфраструктура компанії (не symplexy)
**Розпочато:** 2026-08-01

## Крок

**Поточний:** завершено — 8/8
**Виконано:**
1. ✅ Аудит, 3 частини (`report.md`, `report-part2-sessions.md`, `report-part3-hermes-source.md`)
2. ✅ `company/protocols/source-of-truth.md` — ієрархія джерел правди
3. ✅ `decisions.md` — статуси + ретрофіт 7 записів
4. ✅ `company/workspace/ACTIVE.md` — цей файл
5. ✅ `session-brief.sh` читає `source`; `precompact.sh` + `settings.json` — протестовано
6. ✅ `CLAUDE.md` 211 → 103 рядки; оркестраторське перенесено в `orchestration.md`
7. ✅ Памʼять через кандидатів (`definition-of-done.md`)
8. ✅ Сканер фактів бізнесу у файлах ремесла (`memory-guard.sh`) — протестовано в обидва боки

**Далі:** нічого — задача закрита.

**Не робимо** (засновник не обрав): `disallowedTools` / `maxTurns` у frontmatter 21 працівника.

## Незворотне вже зроблене

<!-- КРИТИЧНИЙ РОЗДІЛ. Усе, що не можна виконати вдруге:
     публікації, витрати, зовнішні запити, листи, зміни в проді, видалення.
     Порожньо = нічого незворотного не робилось. Не залишати порожнім за здогадом. -->

- Пуш у `origin/claude/agent-hypothesis-audit-plrzr9` (двічі). Оборотне, але вже на віддаленому.
- Грошей не витрачено, нічого не опубліковано назовні, нічого не видалено.

## Блокери й відкриті питання

**Найбільша дірка:** `company/projects/symplexy/founder-context.md` порожній —
працівники не знають бізнесу. Заповнити з засновником після цієї задачі.

## Наступна безпечна дія

Заповнити `company/projects/symplexy/founder-context.md` разом із засновником.

---

## Як заповнювати

Оркестратор переписує цей файл:

- на старті задачі — заповнює всі розділи;
- при переході до наступного працівника — оновлює «Крок»;
- **одразу після незворотної дії** — дописує в «Незворотне», не відкладаючи;
- при завершенні — скидає до шаблону.

Правило одне: **розділ «Незворотне» пишеться до того, як дія виконана, а не після.**
Якщо сесія обірветься між дією і записом, наступна повторить публікацію або витрату.

Формат вільний, головне — щоб нова сесія за 10 секунд зрозуміла, де ви спинились.
