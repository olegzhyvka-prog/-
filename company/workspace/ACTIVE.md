# Активний стан

Що виконується **зараз**. Один файл, переписується оркестратором при зміні кроку.

`SessionStart`-хук читає його і вливає в контекст нової сесії. `Stop`-хук комітить його
в git після кожного ходу — тому обірвана сесія не втрачає точку продовження.

**Це не журнал.** Історія — в git. Тут завжди лише поточне.

Коли задача завершена — скинути до шаблону («Задача: немає»).

---

---

## Задача

**Задача:** Symplexy — визначити нове позиціювання каталогу+асистента і по-новому подати
його всюди (сайт, реклама, комунікація), щоб на продукті можна було заробляти
**Гілка сесії:** `claude/symplexy-ai-strategy-value-pnwvda` (перейменована 2026-08-03,
див. «Незворотне»)
**Проєкт:** symplexy (бізнес засновника)
**Розпочато:** 2026-08-03

**Паралельний, окремий трек (не ця задача):** підсилення оркестратора за Hermes Agent —
стан не змінювався цією сесією, деталі нижче в «Архів попереднього кроку».

## Крок

**Поточний:** `product-strategist` завершив розбір позиціювання —
`company/projects/symplexy/audits/2026-08-03-positioning-strategy.md`. Діагноз: монетизацію
ніхто не починав (продукт побудований для безкоштовної сторони, платника — нуль). 4 варіанти
позиціювання (A цех-вертикаль / B рекламний канал / C вбудова в чужу платформу / D дані —
опціон, не варіант). 🔴 Терміново: поточна реклама Meta запускається під арифметику, де
ціна відвідувача перевищує його дохід у ~7 разів (базовий сценарій, усі числа — [П]).
Чекаю відповідь засновника на 5 питань зі звіту (Q27, який варіант перевіряти, чи готовий
особисто продавати, статус «безкоштовно назавжди» — принцип чи гіпотеза, Q25 + реальні
цифри реклами).

**Що зроблено цією сесією:**
1. ✅ Записано правило звертання на ім'я + план дій перед кожною дією —
   `company/protocols/orchestration.md`
2. ✅ Знайдено і зведено в `company/projects/symplexy/founder-context.md`: контекст бізнесу
   з `2sh3ms` (538 рядків, 2026-08-01/02) + новіші факти з `gwak6k` (2026-08-03) —
   тверді межі (без темних фонів, палітру не чіпати), два відхилені прототипи, готовий
   8-етапний план редизайну (не запускається — див. нижче), реклама вже запускається,
   блокування домену в мережі середовища.
3. ✅ Перенесено `QUESTIONS.md`, `BRIEF-FOR-OTHER-CHAT.md`, `sources/`, `ledger/decisions.md`,
   `ledger/lessons.md`, `audits/2026-08-03-*`, `audits/landing-proto/`, `audits/landing-v2/`
   на робочу гілку.
4. ✅ Урок у `company/ledger/lessons.md` (2026-08-03): нові сесії створюють гілки з
   випадковими іменами, які нічого не дзеркалить — перевіряти всі гілки origin перед тим,
   як вважати проєктний контекст порожнім.
5. ✅ Скоуп вирішено засновником: широка задача (позиціювання всього продукту), не вузька
   (тільки лендинг). Паралельна сесія `gwak6k`/`analysis-requirements-gwak6k` отримала
   вужчу постановку раніше — якщо та сесія ще активна, зважати при злитті.
6. ✅ Гілки перейменовано з помилкового «simplex» на правильне «symplexy» (назва продукту —
   `symplexy.ai`, звірено з `founder-context.md`). Деталі — «Незворотне».

**Далі:** дочекатись результату `product-strategist` (варіанти позиціювання), звірити із
засновником, потім передати відповідному працівнику (copywriter/brand-designer) на подачу.
Старий `audits/2026-08-03-plan-redesign.md` — не запускати як є, він написаний під старе
позиціювання; переглянути після нового позиціювання.

## Незворотне вже зроблене

<!-- КРИТИЧНИЙ РОЗДІЛ. Усе, що не можна виконати вдруге:
     публікації, витрати, зовнішні запити, листи, зміни в проді, видалення.
     Порожньо = нічого незворотного не робилось. Не залишати порожнім за здогадом. -->

- **2026-08-03, за прямою вказівкою засновника:** перейменовано гілку сесії
  `claude/simplex-ai-strategy-value-pnwvda` → `claude/symplexy-ai-strategy-value-pnwvda`
  (стару видалено на remote, успішно). Спроба так само перейменувати чужу застиглу гілку
  `claude/simplex-ai-strategy-value-sy847u` (сесія завершена, вміст ідентичний дефолтній
  гілці, нічого унікального) — вдалось лише *створити* нову
  `claude/symplexy-ai-strategy-value-sy847u`; видалити стару чи навіть щойно створену не
  вдалось — proxy повертає 403 на будь-яке видалення гілки, не лише чужої. Результат: на
  remote тепер лишились **обидві** назви для sy847u (`simplex-...` і `symplexy-...`),
  видалити їх може тільки засновник напряму через GitHub. Не блокер — обидві порожні
  дублікати без унікального вмісту.
- Грошей не витрачено, назовні нічого не опубліковано, дані не видалено.

## Блокери й відкриті питання

**Далі не заповнене в `founder-context.md`:** скільки годин/тиждень, дедлайн трьох робіт,
роль другого co-founder — засновник свідомо відклав ці питання 2026-08-02, не перепитувати.

**Прибирання гілок:** дублікат `claude/simplex-ai-strategy-value-sy847u` /
`claude/symplexy-ai-strategy-value-sy847u` не видаляється з цієї сесії (403 на delete) —
якщо засновник хоче прибрати остаточно, треба видалити вручну на GitHub.

## Наступна безпечна дія

Чекаю відповідь засновника на 5 питань зі звіту позиціювання. До відповіді: не запускати
жоден із варіантів A/B/C/D, не чіпати `plan-redesign.md`, не зупиняти й не масштабувати
рекламу самостійно (це рішення засновника, хоч і з попередженням про арифметику вище) —
можу передати `paid-ads-buyer`/`finance-controller` зняти фактичні цифри поточного запуску,
якщо засновник підтвердить.

## Архів попереднього кроку (Hermes, не чіпалось цією сесією)

Етап 1 закритий (8/8), чекає новий матеріал від засновника про Hermes Agent. Робоча тека
`company/workspace/2026-08-01-hermes-audit/`. Деталі — `report.md` там-таки. Свідомо НЕ
зроблено: `disallowedTools`/`maxTurns` у frontmatter (засновник не обрав, пропозиція чинна
в `report.md`, розділ C1).

---

## Як заповнювати

Оркестратор переписує цей файл:

- на старті задачі — заповнює всі розділи;
- при переході до наступного працівника — оновлює «Крок»;
- **одразу після незворотної дії** — дописує в «Незворотне», не відкладаючи;
- при завершенні — скидає до шаблону.

Правило одне: **розділ «Незворотне» пишеться до того, як дія виконана, а не після.**
Якщо сесія обірветься між дією і записом, наступна повторить публікацію або витрату.

Формат вільний, головне — щоб нова сесія за 10 секунд зрозуміла, де ви спинились.
